<Window x:Class="RealEstateDirectory.Shell.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Shell="clr-namespace:RealEstateDirectory.Shell"
        mc:Ignorable="d" Title="ShellView" Height="300" Width="300"
        d:DataContext="{d:DesignInstance Shell:ShellViewModel}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Menu Grid.Row="0">
			<MenuItem Header="Файл">
				<MenuItem Header="Выход" Command="{Binding ExitCommand}" />
			</MenuItem>
			<MenuItem Header="Справочники">
				<MenuItem Header="Улицы" Command="{Binding StreetsDictionaryCommand}" />
				<MenuItem Header="Виды сделок" Command="{Binding DealVariantsDictionaryCommand}" />
                <MenuItem Header="Планироваки Layout экспериментальные" Command="{Binding LayoutDictionaryCommand}" />
		</MenuItem>
		</Menu>
	</Grid>
</Window>
